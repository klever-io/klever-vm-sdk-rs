# Klever Data Encoding & Handling

The crates in this folder deal with data manipulation.

They are as follows:
    - `klever-sc-codec` - the standard serializer/deserializer for SC data
    - `klever-sc-codec-derive` - procedural macros for `klever-sc-codec`
